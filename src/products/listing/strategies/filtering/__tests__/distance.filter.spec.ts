import { DistanceFilter } from '../../filtering/distance.filter';

describe('DistanceFilter', () => {
  it('compiles and can be constructed', () => {
    expect(typeof DistanceFilter).toBe('function');
  });
});
