import { DistanceSort } from '../../sorting/distance.sort';

describe('DistanceSort', () => {
  it('compiles and can be constructed', () => {
    expect(typeof DistanceSort).toBe('function');
  });
});
