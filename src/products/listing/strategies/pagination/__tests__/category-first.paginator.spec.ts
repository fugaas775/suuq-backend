import { CategoryFirstPaginator } from '../../pagination/category-first.paginator';

describe('CategoryFirstPaginator', () => {
  it('compiles and can be constructed', () => {
    expect(typeof CategoryFirstPaginator).toBe('function');
  });
});
